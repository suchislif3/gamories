import { makeStyles } from "@material-ui/core";

export default makeStyles({
  postContainer: {
    position: "relative",
    minHeight: "440px",
    boxSizing: "border-box",
    padding: 0,
  },
});
