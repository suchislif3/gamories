import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  appBarSearch: {
    borderRadius: "16px",
    marginBottom: "1rem",
    display: "flex",
    padding: "16px",
  },
  chipInput: {
    margin: "10px 0",
  },
}));
